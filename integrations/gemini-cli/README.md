# Gemini CLI integration

```bash
reposeal run -- gemini
```

Add the RepoSeal MCP stdio server from [`../mcp/config.example.json`](../mcp/config.example.json). The wrapper catches `git`, package managers, Go/Cargo, and direct downloads spawned by Gemini CLI, while MCP supplies a structured voluntary verification tool.

