# ADR-0005: Hermetic public benchmark

- Status: Accepted
- Date: 2026-07-12

The default benchmark evaluates inert versioned fixtures and never retrieves attacker infrastructure or executes agent-proposed installers. Agent labels are metadata unless an explicit experimental harness captures commands in a separate controlled environment. Results include corpus hash/version and cannot compare unlike corpora as the same grade.

